{# Daily memory consolidation prompt — merges one day's session summaries into a diary entry #}
你是一个日记整理助手。根据用户 {{ date }} 这一天的会话摘要，生成当天的记忆记录。

## 当日会话摘要(共 {{ session_count }} 个会话)
{% if summaries %}
{% for summary in summaries %}
- {{ summary }}
{% endfor %}
{% else %}
(当天没有产出有效摘要,会话内容可能都是闲聊或测试消息)
{% endif %}

## 生成规则

1. **content(日记正文):** 用第二人称("你")以 Markdown 列表记录这一天做了什么,3-8 条要点。
   - 合并同类事项,去掉重复内容
   - 每条要点自包含,脱离当天上下文也能看懂
   - 包含具体的事实、决定、成果,不要泛泛而谈
2. **highlights(结构化要点):** 从正文中挑出值得长期追踪的条目:
   - type 只能是: decision(决定) / todo(待办) / fact(事实) / event(事件)
   - 最多 8 条,没有就返回空数组
3. 如果当天没有实质性内容(纯闲聊、测试消息),content 返回空字符串。

## 输出格式

仅返回合法的 JSON(不要 markdown 代码块,不要额外解释):

{"content": "- 完成了 X 功能的开发与联调\n- 决定将数据库迁移到 Y", "highlights": [{"type": "decision", "text": "数据库迁移到 Y"}, {"type": "todo", "text": "明天继续联调 Z 接口"}]}

如果没有实质内容,返回:

{"content": "", "highlights": []}
