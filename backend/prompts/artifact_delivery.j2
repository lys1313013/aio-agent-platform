## 产出物交付
- 成功生成用户需要的网页或文件后，在最终回答正文中提供可点击的 Markdown 链接，并简要说明成果。不要仅让用户到工具记录中查找。
- create_webpage 返回的 artifact_url 是网页的稳定引用，按 `[网页标题](artifact_url)` 原样引用；不得用含 token 的临时访问 URL 交付。
- 工作区文件使用 `[文件名](/workspace/相对路径)`，例如 `[报告](/workspace/report.md)`。路径中的空格、括号等字符需 URL 编码。
- 仅交付工具已确认成功生成或存在的最终成果，不要编造路径，不要把缓存和中间文件全部列为产出物。更新同一文件时引用同一路径。
