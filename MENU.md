# 菜单功能说明

面向智能体开发者，说明每个菜单模块为智能体提供什么能力。

## 智能体

智能体是核心运行单元——一个配置了模型、提示词、工具、技能、知识库的 LLM 实例。

- **创建/编辑智能体**：配置基础信息（名称、图标、描述）
- **系统提示词**：定义智能体的行为准则、角色边界、输出格式。支持模板加载
- **模型选择**：为每个智能体指定不同的大模型（可覆盖全局默认）
- **推理温度 & 最大迭代次数**：控制智能体的随机性（0-2）和单轮工具调用循环上限
- **欢迎语 & 快捷提问**：会话初始引导，降低使用门槛
- **自动记忆提取**：对话结束后自动抽关键信息写入记忆，后续会话可检索
- **LLM 自动重试**：API 调用失败时指数退避重试（最多 3 次）
- **子智能体**：将其他智能体作为子级，当前智能体可向其委派任务（多智能体协作）

> 智能体卡片上的标签展示其已绑定的模型、内置工具数、MCP 服务数、子智能体数。

## 工具配置

为智能体选择可调用的内置工具。工具按类别分组：

| 类别 | 说明 |
|------|------|
| 沙箱执行 | 在隔离沙箱中运行代码（Python/Bash） |
| 文件 | 读写工作区文件 |
| 知识库 | 从绑定的知识库检索内容 |
| 记忆 | 读写 L1/L2/L3 层记忆 |
| 技能 | 激活和调用已绑定的技能 |
| 多智能体 | 委派任务给子智能体 |
| 交互 | 用户确认、AskUserQuestion |
| 远程工具 | 调用外部 HTTP API |
| 其他 | 额外的系统工具 |

> 部分工具（如知识库检索、委派任务）在绑定知识库或子智能体后自动注入，无需手动勾选。

## MCP 服务

接入外部 MCP（Model Context Protocol）服务，让智能体调用第三方提供的工具。

- **添加 MCP 服务**：支持 SSE 和 Streamable HTTP 传输
- **工具前缀**：为服务下所有工具名加统一前缀，避免名称冲突
- **逐工具选择**：可精确控制每个 MCP 工具是否对智能体可用
- **连接状态**：实时展示服务的连接状态（已连接 / 错误 / 未连接）

> MCP 工具与内置工具分开管理——MCP 工具的启用状态随 MCP 服务配置保存，不会混入内置工具计数。

## 技能

管理和部署可复用的技能包。技能是一组带触发条件的脚本和参考文件，智能体在对话中自动匹配并调用。

- **创建技能**：编写 Markdown 格式的技能内容，定义名称、分类、标签、触发条件
- **版本管理**：每次修改自动递增版本号，可回溯历史版本
- **文件管理**：上传脚本（Python/Bash）、参考文档、静态资源
- **导入/导出**：技能包可打包为 zip 分发和迁移
- **使用统计**：追踪每个技能的调用次数和成功率
- **搜索匹配**：后端根据触发条件和标签自动匹配最相关的技能

## 知识库

绑定外部知识源，为智能体提供 RAG（检索增强生成）能力。

- **添加知识库**：关联 RAGFlow 知识库（通过 dataset_id）
- **测试检索**：直接在页面上测试检索效果，查看召回内容
- **绑定到智能体**：在智能体配置侧边栏中选择知识库，自动注入检索工具

## 远程工具

将外部 HTTP API 封装为智能体可调用的工具。

- **HTTP 方法**：支持 GET / POST / PUT / DELETE / PATCH
- **认证方式**：无认证、Bearer Token、API Key、Basic Auth、自定义 Header
- **参数定义**：JSON Schema 描述输入参数，自动生成函数签名
- **响应提取**：支持从 API 响应中提取指定字段
- **在线测试**：填写参数后直接测试接口可用性
- **启用开关**：可随时禁用某个远程工具而不必删除

> 远程工具属于内置工具的一种（category: `remote`），在工具配置中统一管理。

## 模型管理

管理 LLM 供应商和模型列表。

- **供应商管理**：添加 OpenAI 兼容的 API 供应商（base URL + API Key）
- **模型注册**：手动添加或一键从供应商拉取可用模型列表
- **默认模型**：设置全局默认模型（智能体未指定模型时使用）
- **多模态支持**：标记模型是否支持图像输入
- **启用/禁用**：控制模型可用性而不必删除

## 对话

智能体的交互入口，通过聊天界面与智能体对话。

- **会话管理**：创建、重命名、置顶、归档、删除对话
- **实时流式响应**：WebSocket 推送，实时展示思考过程、工具调用、最终回答
- **工具调用可视化**：展开查看每个工具调用的参数和结果
- **用户确认**：当智能体调用危险工具或需要选择时，弹出确认面板
- **委派追踪**：子智能体被委派任务后，展示其思考过程和执行结果
- **附件/文件**：上传图片或文件供智能体处理
- **API 文档**：每个智能体可导出对外 API（Chat/Stream/Agent），附带 SDK 示例代码

## 记忆

查看和管理智能体产生的记忆内容。

- **三层记忆模型**：
  - L1 会话内短期记忆
  - L2 跨会话中期记忆（自动提取+手动创建）
  - L3 长期知识记忆
- **语义搜索**：在记忆中检索相关内容
- **CRUD**：手动创建、编辑、删除记忆条目

## 技能搜索

跨所有技能进行语义搜索，查找匹配特定任务需求的技能。
