# 17-用户与租户隔离

平台是多租户架构。每张业务表按归属维度分为三类：**租户级**（tenant_id，租户内共享）、**用户级**（user_id，仅本人可见）、**全局级**（无归属字段）。

隔离的实现方式是**应用层查询过滤**（SQLAlchemy where 条件），数据库并未启用 PostgreSQL RLS policy（见下文「隔离机制」）。

## 一、归属维度总表

| 功能模块 | 表 | 归属字段 | 维度 | 附加控制 |
|---|---|---|---|---|
| 智能体 | `agents` | `tenant_id` + `created_by` | 租户级 | `visibility`: tenant/private |
| 知识库 | `knowledge_bases` | `tenant_id` + `created_by` | 租户级 | `visibility`: tenant/private |
| MCP 服务器 | `mcp_servers` | `tenant_id` | 租户级 | — |
| IM 渠道配置 | `channel_configs` | `tenant_id` + `created_by` | 租户级 | — |
| 渠道用户绑定 | `channel_bindings` | `tenant_id` + `user_id` | 租户级 | 绑定目标指向具体用户 |
| 渠道绑定码 | `channel_bind_codes` | `tenant_id` | 租户级 | — |
| 渠道会话映射 | `channel_session_mappings` | 无归属字段 | 跟随渠道 | 通过 channel_id 间接归属 |
| 宠物包 | `pet_packages` | `tenant_id` + `owner_id` | 租户级 | `visibility`: private/tenant/public/official |
| 会话 | `sessions` | `user_id` | 用户级 | — |
| 消息 | `messages` | `user_id` | 用户级 | — |
| 记忆 | `memories` | `user_id` | 用户级 | 仅本人（L1/L2/L3） |
| 技能 | `skills` | `user_id` | 用户级 | — |
| 工作空间（文件存储） | `workspaces` | `user_id` | 用户级 | 仅本人 |
| 定时任务 | `cron_jobs` | `tenant_id` + `user_id` | 租户级 | 关联可空 `agent_id` |
| 定时任务运行记录 | `cron_job_runs` | `tenant_id` + `user_id` | 租户级 | — |
| 用户画像 | `user_profiles` | `user_id` | 用户级 | — |
| 画像版本 | `portrait_versions` | `user_id` | 用户级 | — |
| 用户配置 | `user_configs` | `user_id` | 用户级 | — |
| 刷新令牌 | `refresh_tokens` | `user_id` | 用户级 | — |
| 追踪 | `traces` | `user_id` | 用户级 | — |
| 沙箱会话 | `sandbox_sessions` | `user_id` | 用户级 | — |
| 用量统计 | `token_usage_daily` | `user_id` | 用户级 | — |
| 用户宠物实例 | `user_pets` | `user_id` | 用户级 | — |
| 宠物经验流水 | `pet_exp_logs` | `user_id` | 用户级 | — |
| 委派记录 | `delegations` | `user_id` | 用户级 | — |
| 审计日志 | `audit_logs` | `user_id`（可空） | 用户级 | — |
| 租户 | `tenants` | — | 全局级 | 平台级主体 |
| 租户成员 | `tenant_memberships` | `tenant_id`+`user_id` | 全局级 | 租户-用户多对多 |
| LLM 提供商 | `llm_providers` | — | 全局级 | 所有租户共用 |
| LLM 模型 | `llm_models` | — | 全局级 | 所有租户共用 |
| 远程工具 | `remote_tools` | — | 全局级 | 无归属字段 |
| 系统配置 | `system_config` | — | 全局级 | 平台级配置 |

关联表（无独立归属，跟随主资源）：`agent_relationships`、`agent_skills`、`agent_knowledge_bases`、`agent_versions`（跟随 agent）、`skill_versions`（跟随 skill）。

## 二、租户级与用户级的区别

**租户级资源**（有 `tenant_id`）：
- 属于整个租户，租户内所有成员共享
- 配合 `visibility` / `created_by` 做二级控制（见第三节）
- **跨租户完全不可见**——`tenant_id == 当前用户所属租户` 是硬性过滤条件
- 典型：智能体、知识库、MCP、IM 渠道、宠物包

**用户级资源**（只有 `user_id`）：
- 严格属于单个用户，无任何共享机制
- 永远按 `WHERE user_id = 当前用户` 过滤
- 即使同租户的另一成员也看不到
- 典型：会话/消息、记忆、定时任务、工作空间、用户宠物、用量

**全局级资源**：
- 无归属字段，所有租户、所有用户可见
- 典型：LLM 提供商/模型、远程工具、系统配置、租户主体本身

## 三、visibility 可见性控制

租户级资源内部用 `visibility` 字段细分可见范围。

**Agent / KnowledgeBase**（值域：`tenant` / `private`，默认 `tenant`）：
- `tenant`：租户内所有成员可见、可选用（默认）
- `private`：仅 `created_by` 创建者本人可见

列表查询的实际过滤条件（`agents.py`）：
```python
(Agent.tenant_id == user.tenant_id) & or_(
    Agent.visibility == "tenant",
    Agent.created_by == user.id,
)
```

**PetPackage**（值域：`private` / `tenant` / `public` / `official`，默认 `private`）：
- `private`：仅创建人（`owner_id`）
- `tenant`：租户内共享
- `public`：跨租户公开
- `official`：平台官方包（管理员维护）

## 四、用户归属租户

- `users.tenant_id`：用户的主租户（必填，`DEFAULT_TENANT_ID` 兜底）
- `tenant_memberships`：用户与租户的多对多关系，支持一个用户属于多个租户
- 代码中过滤时统一取 `user.tenant_id` 作为「当前租户」

## 五、隔离机制

- 应用层：所有路由在查询时显式拼接 `tenant_id` / `user_id` 过滤条件（SQLAlchemy where）。这是实际的隔离手段。
- 数据库层：`db/connection.py` 的 `get_db()` 会执行 `SET LOCAL app.current_user_id = '<uid>'`，把当前用户 ID 写入 PostgreSQL 会话变量。
- **注意**：代码库中未找到任何 `CREATE POLICY` / `ENABLE ROW LEVEL SECURITY`，即数据库 RLS policy 并未真正启用。`app.current_user_id` 会话变量目前没有被 policy 消费，多租户隔离完全依赖应用层过滤。
- 后台执行上下文（无 HTTP 请求、无 `get_db` 依赖）会由代码显式设置 current_user_id：定时任务在 `scheduler.py` / `handlers.py` 中执行 `select(func.set_config("app.current_user_id", str(job.user_id), True))` 并 `current_user_id.set(...)`；渠道事件回调在 `channels/pipeline.py` 中同样设置。即所有涉及数据库操作的执行路径都以「当前用户」身份运行。

## 六、新表归属判定指南

新增功能时，按以下问题确定归属维度：

1. 该资源需要被**同一租户内多个用户共享**吗？
   - 需要 → 租户级：加 `tenant_id` 字段；若还要区分「仅创建者可见」→ 加 `visibility` + `created_by`
   - 不需要，仅本人 → 用户级：加 `user_id` 字段
2. 需要跨租户可见吗？（如平台公开内容）→ 在租户级基础上扩 visibility 值域，如宠物包的 `public`/`official`
3. 需要全局唯一、所有租户共用吗？（如 LLM 配置、系统设置）→ 全局级，不加归属字段

判定后：租户级表加 `Index(tenant_id)`；用户级表加 `Index(user_id)`；路由查询务必拼接对应过滤条件，不要遗漏。
